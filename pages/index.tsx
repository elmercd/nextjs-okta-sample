import type { NextPage } from "next";
import LoginButton from "../components/login-btn/login-btn";

const Home: NextPage = () => {
  return (
    <div >
      <LoginButton />
    </div>
  );
};

export default Home;
